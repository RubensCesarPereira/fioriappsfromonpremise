<!DOCTYPE xml>
<mvc:View controllerName="liberarPrecioExp.controller.app"
	displayBlock="true" height="100%" xmlns="sap.m" xmlns:core="sap.ui.core"
	xmlns:f="sap.f" xmlns:mvc="sap.ui.core.mvc">
	<App id="RootView" busy="{appView>/busy}" backgroundImage="{background>/img}"
		busyIndicatorDelay="{appView>/delay}">
		<f:FlexibleColumnLayout id="layout" layout="{appView>/layout}"
			backgroundDesign="Solid">
		</f:FlexibleColumnLayout>
	</App>
</mvc:View>